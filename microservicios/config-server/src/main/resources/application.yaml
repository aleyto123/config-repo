server:
  port: 8888

spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: file:///C:/Users/ronyc/git/config-repo
          default-label: main
          search-paths: config-repo
